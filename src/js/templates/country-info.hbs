{{#each this}}

  <h2>
    <img src='{{flags.svg}}' alt='{{name.common}}' />

    {{name.official}}
  </h2>
  <p>Capital: {{capital}}</p>
  <p>Population: {{population}}</p>
  <p>Languages:
    {{#each languages}}
      {{this}}{{#unless @last}}, {{/unless}}
    {{/each}}</p>

{{/each}}